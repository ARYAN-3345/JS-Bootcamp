const getSum = function(a,b)
{
    return a * b;
}

const sum = getSum(15, 20);
console.log(`The sum is: ${sum}`);  // This will log the sum to the console

const mulitply = (x,y) => x* y;
console.log(`The product is: ${mulitply(5, 6)}`);  // This will log the product to the console

// This does the similar hting as the above function
// function multi(x,y){
//     const c = x * y;
//     return c;
// }