let counter = 0;
while (counter < 3) {
    console.log(`Loop iteration: ${counter}`);
    counter = counter +1; //counter +=1 or counter++ can also be used
    // This will log the current iteration number;
}
console.log("While loop finished");