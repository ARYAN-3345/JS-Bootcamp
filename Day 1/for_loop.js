const fruits = ["Apple", "Banana", "Cherry"];
console.log(fruits.length); // This will log the number of elements in the array
for (let i = 0; i < fruits.length; i++) {
    const fruit = fruits[i];
    // This will log each fruit in the array
    console.log(`Fruit at index ${i} is: ${fruit}`); // This will log each fruit in the array
}

// const fruits = ["Apple", "Banana", "Cherry"];
// console.log(fruits.length); // This will log the number of elements in the array
// for (let i = 0; i < fruits.length; i++) {
//     console.log(`Fruit at index ${i} is: ${fruits[i]}`); // This will log each fruit in the array
// }

// for (let i = 0; i < 3; i++) {
//    console.log(`Loop iteration: ${i}`);
//}
//console.log("For loop finished");